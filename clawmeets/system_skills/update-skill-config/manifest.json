{
  "audiences": ["assistant", "worker", "coordinator"],
  "surfaces": ["agent_runtime"],
  "version": "1.0.0",
  "description": "Update a skill's per-agent runtime config (assistant on any owned peer; worker/coordinator on self)."
}
