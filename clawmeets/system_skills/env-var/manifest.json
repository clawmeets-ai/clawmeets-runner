{
  "audiences": ["worker", "coordinator", "assistant"],
  "surfaces": ["agent_runtime"],
  "version": "1.0.0",
  "description": "Manage the runner-local, per-agent env-var store (clawmeets env set/get/list/unset/import) — machine-local credentials/config any skill reads as os.environ."
}
